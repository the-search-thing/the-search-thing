# todo

- [x] [DEBUG]file paths not being returned when search all was made and search videos was combined into it
- [ ] generating random uuid may create duplicate nodes if same video is indexed multiple times, use hash of video path or checking for video ndoes first
- [x] we need to add the file path to schema and queries  and return that as well.
- [x] we need to send the job status first to frontend 
- [ ] we need to send the llm results with filepath & not all helix results
